const textToResponse = require('./utils/text-to-response');

const main = async () => {
    try {
        //const response = await textToResponse();
        console.log(response)
    }
    catch (error) {
        console.log(error);
    }
}

main();